import 'package:flutter/material.dart';

class HomeViewTableLayout extends StatelessWidget {
  const HomeViewTableLayout({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("from the tablet ya bro"),
    );
  }
}