import 'package:flutter/material.dart';

class BillingsMobileView extends StatelessWidget {
  const BillingsMobileView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("billing mobile view"),
    );
  }
}